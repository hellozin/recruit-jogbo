spring:
  jpa:
    show-sql: true # show query generated by JPA
  h2:
    console:
      enabled: true
      path: /h2-console
logging:
  level:
    org.hibernate.type: trace # Show parameter of generated JPA query